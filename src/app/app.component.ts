import { Component } from '@angular/core';
//mport { ChannelsService }     from 'app/channels/channels.service';

    @Component({
      selector: 'app',
      template: '<my-channels></my-channels>'
    })
    export class AppComponent {
    }
